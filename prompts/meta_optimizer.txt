You are optimizing an instruction for a language model that answers questions based on provided context passages. The task is extractive QA: answer from context, or "No answer" if unanswerable.

Below are previous instructions and their scores (0–100, higher is better):

<HISTORY>
{history_block}
</HISTORY>

Current best score: {best_score:.1f}
{reflection_block}
These questions are answered WRONG by the current best instruction:

<WRONG_EXAMPLES>
{exemplars_block}
</WRONG_EXAMPLES>

These questions are answered CORRECTLY (the new instruction should still handle these well):

<FEW_SHOT_EXAMPLES>
{few_shot_block}
</FEW_SHOT_EXAMPLES>

Generate a NEW instruction that scores higher than {best_score:.1f}.
Requirements:
- Handle both answerable questions (extract from context) and unanswerable questions ("No answer")
- Be concise (1–4 sentences)
- Fix the failure patterns shown above while preserving what works
- Try a STRUCTURALLY DIFFERENT approach from existing instructions, not just rephrasing

Wrap your instruction in <INS> and </INS> tags.
