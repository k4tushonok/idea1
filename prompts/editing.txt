You are an expert prompt engineer. Your task is to improve a prompt based on analysis of its failures.

## CURRENT PROMPT:
```
{current_prompt}
```
## FAILURE EXAMPLES (what the current prompt produces incorrectly):
{failures_block}

## ANALYSIS OF PROBLEMS:
{error_analysis}

## SUGGESTED DIRECTION FOR IMPROVEMENT:
{suggested_direction}

## SPECIFIC SUGGESTIONS:
{specific_suggestions_block}

## MANDATORY CONSTRAINTS:
1. Do NOT just rephrase/paraphrase the current prompt. PARAPHRASING = FAILURE.
   If your variant says the same thing in different words, it is REJECTED.
2. Each variant MUST introduce a NEW structural element not present in the current prompt.
   Examples of valid structural changes:
   - Add an explicit output format constraint (e.g., "Reply with only the exact text span")
   - Add a decision rule for edge cases (e.g., "If the question cannot be answered from the context, respond with exactly 'No answer'")
   - Add step-by-step reasoning (e.g., "First locate the relevant sentence, then extract the answer")
   - Add disambiguation rules (e.g., "If multiple answers are possible, choose the most specific one")
   - Change the task framing (e.g., from "answer the question" to "extract the minimal text span that answers")
3. Do NOT copy specific values, names, numbers from the failure examples into the prompt.
4. The output must be a prompt FOR the language model, not a description of how to edit a prompt.
5. Keep prompts concise (1-5 sentences). A focused short prompt beats a long unfocused one.
6. Preserve any output format requirements from the current prompt.
7. Inside each PROMPT code block, start directly with the prompt text.

## YOUR TASK:
Generate {num_variants} improved versions. Each must have a genuinely different structure.

## OUTPUT FORMAT:
For each variant, use this exact format:
VARIANT [number]:
CHANGES MADE: [What NEW structural element you added and why it fixes the failures]
PROMPT:
```
[The complete improved prompt here]
```

Now generate {num_variants} variants: