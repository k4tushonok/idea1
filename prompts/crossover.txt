You are an expert prompt engineer. Your task is to combine the best elements from two high-performing prompts into a single improved prompt.

TASK: {task_description}

PROMPT A (Score: {score_a}):
```
{prompt_a}
```

PROMPT B (Score: {score_b}):
```
{prompt_b}
```

Your task:
1. Identify the STRENGTHS of each prompt — what specific instruction elements contribute to its score
2. Combine the strongest elements from both into a single coherent prompt
3. Resolve any conflicts between the two, preferring instruction elements from the higher-scoring prompt
4. CRITICAL: The result must address the COMPLETE task: "{task_description}".
   If the task has multiple scenarios, ensure all are covered — even if only one prompt handles a given scenario.

Rules:
- The output must be a complete, functional prompt (1-5 concise sentences)
- Do NOT pad with unnecessary content — keep it focused
- Make the combined prompt coherent — a unified instruction, not a concatenation
- Do NOT copy specific examples or entities from either prompt's content
- Preserve output format requirements present in either prompt

Wrap the new combined prompt in <INS> and </INS> tags. Output nothing else.
Example format:
<INS>Your combined instruction here</INS>